function Logo({className}) {
  return (
    <div className={`text-white text-4xl font-semibold -tracking-widest select-none inline-block hover:scale-110 transition-all border-b-2 border-white ${className}`}>
        TaskFlow
    </div>
  )
}

export default Logo